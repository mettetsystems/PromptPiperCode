# Frontend tests (`src/test/`)

| File | Role |
|------|------|
| `setup.ts` | Vitest + jsdom global setup |

Unit tests for libs and components live next to their sources (`*.test.ts` / `*.test.tsx`).

```bash
# Run all web tests from repo root
make test-web
```
