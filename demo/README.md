# Demo fixtures (`demo/`)

Static input for the implementation-report demo scenario.

| File | Role |
|------|------|
| `implementation_report.yaml` | Sample requirement narrative used by `make demo` |

Runtime outputs land under `data/demo/` (registry, artifacts, audit) — see [data/README.md](../data/README.md).

```bash
# Run the scripted demo from repo root
make demo
```
